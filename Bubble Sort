# Bubble Sort

lista = [10, 3, 7, 8, 2]

def bubble_sort(lista):
    n = len(lista)
    for j in range(n-1):
        for i in range(n-1-j):
            if lista[i] > lista[i+1]:
                lista[i], lista[i+1] = lista[i+1], lista[i]


bubble_sort(lista)
print(lista)
